"use client";

// Version buttons hidden per user request.
// All versions (v1, v2, v3, v4, v5) remain directly accessible via URL bar (/v1, /v2, /v3, /v4, /v5).
export function VersionSwitcher({ className }: { className?: string }) {
  return null;
}
